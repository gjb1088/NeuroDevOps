
# Ensure consistent line endings
* text=auto

# Treat Python files as text and ensure they use LF
*.py text eol=lf

# Treat shell scripts as text with LF
*.sh text eol=lf

# Markdown documentation should use UTF-8 and LF
*.md text eol=lf charset=utf-8

# YAML files (e.g., Ansible) also with LF and UTF-8
*.yml text eol=lf charset=utf-8
*.yaml text eol=lf charset=utf-8

# JSON and configuration files
*.json text eol=lf
*.toml text eol=lf
*.ini text eol=lf

# Ignore binary files (no text normalization)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.db binary
